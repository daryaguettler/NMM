# optional per-machine setup (module load, conda activate, extra PATH).
# copy from env.sh.example:  cp slurm/env.sh.example slurm/env.sh
# slurm/env.sh is gitignored.

# optional: pin batch python and skip uv on compute (recommended for Engaging):
# export CORPUS_PYTHON="$HOME/.conda/envs/nmm/bin/python"

# conda/miniforge (uncomment and edit module path for your site):
# module load miniforge/24.3.0-0
# source "$HOME/miniforge3/etc/profile.d/conda.sh"
# conda activate nmm

export PATH="${HOME}/.local/bin:${PATH}"
